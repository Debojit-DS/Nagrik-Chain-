import { generateMockCitizens } from '@utils/generateMockCitizens';

/**
 * 30 mock citizens for the admin citizen registry.
 * Generated lazily and exported as a static array.
 */
export const mockCitizens = generateMockCitizens(30);
